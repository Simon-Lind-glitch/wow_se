# Build-time only. Never shipped to the client.
# 1.x required: the Batches request types live at
# anthropic.types.messages.batch_create_params, and 1.x runs on httpx2.
anthropic>=1.0
PyYAML>=6.0
platformdirs>=4.2

# dev
pytest>=8.0
ruff>=0.6
