# Build-time only. Never shipped to the client.
#
# There is deliberately no LLM SDK here. Translation happens outside this
# toolchain and comes back through `python -m translate --import-file`, which
# validates it. So the whole pipeline runs offline, with no API key and no
# account attached to it.
PyYAML>=6.0

# dev
pytest>=8.0
ruff>=0.6
